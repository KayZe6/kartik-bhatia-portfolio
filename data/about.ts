import type { AboutData } from "./types";

/**
 * S1 in the copy deck: the human beat. First person by design, per CLAUDE.md's
 * voice rules exception, unlike every other content file in this folder.
 *
 * Trimmed to short supporting text beneath the trajectory timeline (see
 * data/timeline.ts) — both sentences kept are verbatim from the original
 * copy deck paragraph, just with the middle descriptive clause and the
 * second paragraph cut, since the timeline now carries that narrative.
 */
export const about: AboutData = {
  paragraphs: [
    "My interest in robotics and aerospace traces back to Interstellar, mainly TARS and CASE. That's probably where it started.",
  ],
};
